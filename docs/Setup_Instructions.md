# Environment Setup

**JDK Version Used:** Java 25.

**Hello World Verification:**
To verify the setup, we created a simple `HelloWorld.java` file, compiled it using `javac HelloWorld.java`, and executed it using `java HelloWorld`. The console successfully output "Hello World", confirming our compiler and runtime environment are correctly added to the system path.